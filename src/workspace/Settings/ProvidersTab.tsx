function ProvidersTab() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100%',
      color: 'var(--text-muted)',
      fontFamily: 'var(--font-mono)',
      fontSize: 'var(--font-size-md)',
      textAlign: 'center',
      padding: '20px',
    }}>
      AI providers will be configurable here in a future update.
    </div>
  );
}

export default ProvidersTab;
